require 'spec_helper'

describe WheelyTest do
  it 'has a version number' do
    expect(WheelyTest::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
